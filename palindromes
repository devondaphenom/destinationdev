def palindromes(arr)
  ind = 0
  palindromes = []

  while ind < arr.length
    if arr[ind].to_s.reverse == arr[ind].to_s
      palindromes.push(arr[ind])
    end

    ind = ind + 1
  end

  palindromes
end
